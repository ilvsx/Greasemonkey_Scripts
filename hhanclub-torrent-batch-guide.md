# HHClub 当前页种子筛选与批量下载使用指南

## 功能简介

这是一个用于 HHClub 种子列表页的油猴脚本，可在当前页面内筛选、勾选并批量下载种子，支持：

- 关键词组合筛选
- 排除已经下载、正在下载或正在做种的种子
- 按种子大小范围筛选
- 按官种状态筛选
- 按 Free 状态筛选
- 全选可见、取消可见、反选可见和清空选择
- 单个种子直接下载为 `.torrent`
- 多个种子打包为一个 ZIP 下载

> 脚本只处理当前页面已经加载的种子，不会自动抓取其他分页。

---

## 安装前提

### 1. 安装油猴扩展

在浏览器中安装以下任一扩展：

- [Tampermonkey](https://www.tampermonkey.net/)（推荐）
- [Violentmonkey](https://violentmonkey.github.io/)
- [Greasemonkey](https://www.greasespot.net/)（仅 Firefox）

### 2. 安装脚本

将 `hhanclub-torrent-batch.user.js` 导入油猴扩展：

1. 打开油猴扩展的管理面板
2. 新建脚本，或选择“从文件导入”
3. 粘贴或导入 `hhanclub-torrent-batch.user.js`
4. 保存并启用脚本

### 3. 登录 HHClub

脚本使用当前页面的 HHClub 登录状态获取种子文件。使用前需确认：

- 已登录 `https://hhanclub.net/`
- 当前账号可以正常点击站点原有的“下载”按钮
- 浏览器未拦截来自 HHClub 的文件下载

---

## 适用页面

脚本匹配所有 HHClub 种子列表页：

```text
https://hhanclub.net/torrents.php*
```

例如：

```text
https://hhanclub.net/torrents.php?cat[]=412
https://hhanclub.net/torrents.php?inclbookmarked=0&incldead=1&spstate=0&page=1
```

打开或刷新种子列表页后，列表上方会出现筛选与批量下载面板，每条种子前方会增加复选框。

---

## 筛选功能

所有启用的筛选条件采用“同时满足”的方式组合。某个种子必须符合全部条件才会显示。

### 1. 关键词筛选

在顶部搜索框中输入关键词。多个关键词以空格分隔，种子必须包含全部关键词。

示例：

```text
1080p h265
```

表示只显示同时包含 `1080p` 和 `h265` 的种子。

关键词匹配范围为当前种子行中显示的标题、简介和状态文字，不区分英文字母大小写。

### 2. 仅显示已选

勾选“仅显示已选”后，只显示已经勾选的种子。

适合在批量下载前复核选择结果。取消勾选即可恢复其他符合过滤条件的种子。

### 3. 排除已下载/正在下载

勾选“排除已下载/正在下载”后，脚本会排除站点标记为以下状态的种子：

- `seeding`：已经下载并正在做种
- `leeching`：正在下载

没有上述进度标记的种子会保留。

> 该判断依赖 HHClub 当前页面提供的状态标记，不会读取本地下载器的数据。

### 4. 种子大小范围

大小输入框统一使用 MB：

| 输入方式 | 效果 |
|----------|------|
| 只填最小值 | 显示大于或等于该大小的种子 |
| 只填最大值 | 显示小于或等于该大小的种子 |
| 同时填写 | 显示处于闭区间内的种子 |
| 两项留空 | 不限制大小 |

例如：

```text
最小：300
最大：800
```

表示筛选 `300 MB ≤ 种子大小 ≤ 800 MB`。

页面中的 KB、MB、GB、TB 会自动换算为 MB 后比较。

### 5. 官种筛选

| 选项 | 说明 |
|------|------|
| 全部 | 不限制是否为官种 |
| 仅官种 | 只显示带“官方”标记的种子 |
| 排除官种 | 只显示非官种 |

### 6. Free 筛选

| 选项 | 说明 |
|------|------|
| 全部 | 不限制促销状态 |
| 仅 Free | 只显示带“免费”标记的种子 |
| 排除 Free | 只显示非 Free 种子 |

> 全站 Free 活动期间，页面内所有种子都可能被标记为 Free，此时选择“排除 Free”会得到 0 条结果。

### 7. 重置过滤

点击“重置过滤”会清除：

- 关键词
- 仅显示已选
- 排除已下载/正在下载
- 最小和最大大小
- 官种状态
- Free 状态

已经勾选的种子不会被清除。如需同时清除选择，请再点击“清空选择”。

---

## 选择种子

每条种子左侧都有一个复选框。选中的种子会显示蓝色边框。

面板提供以下操作：

| 按钮 | 说明 |
|------|------|
| 全选可见 | 勾选当前过滤结果中的全部种子 |
| 取消可见 | 取消当前过滤结果中的全部选择 |
| 反选可见 | 反转当前过滤结果中的选择状态 |
| 清空选择 | 取消当前页面所有种子的选择 |

“全选可见”“取消可见”和“反选可见”只影响当前可见的过滤结果。被过滤隐藏的种子保持原有选择状态。

计数区域显示：

```text
显示 18/100 · 已选 18
```

含义：

- 当前页面共有 100 条种子
- 其中 18 条符合过滤条件并显示
- 当前共选中 18 条，包括可能被过滤隐藏的已选项

---

## 批量下载

### 下载步骤

1. 设置需要的筛选条件
2. 逐项勾选，或点击“全选可见”
3. 检查“已选”数量
4. 点击“批量下载已选”
5. 等待面板完成获取和打包

获取种子时，状态区域会显示：

```text
获取 3/18：种子标题
```

相邻请求间隔为 500 毫秒。单个请求失败时会等待 800 毫秒后重试一次。

### 单个种子

只选择 1 个种子时，脚本直接下载：

```text
种子标题.torrent
```

### 多个种子

选择多个种子时，脚本会：

1. 使用当前 HHClub 登录状态逐个获取 `.torrent`
2. 在浏览器内生成 ZIP
3. 只触发一次文件下载

ZIP 文件名示例：

```text
hhanclub-torrents-20260824-153012.zip
```

解压后，每个 `.torrent` 文件都以对应的种子标题命名。

这种方式可以避免浏览器将连续下载多个文件判定为自动下载并拦截。

### 部分失败

如果部分种子获取失败，脚本仍会下载成功获取的种子，并显示：

```text
完成：成功 15，失败 3；首个错误：……
```

可以根据错误提示检查登录状态、网络或站点权限后重新下载。

---

## 推荐使用流程

例如，需要下载当前页中尚未下载、大小为 300–800 MB 的 1080p 官种 Free 资源：

1. 关键词输入 `1080p`
2. 勾选“排除已下载/正在下载”
3. 大小最小值输入 `300`
4. 大小最大值输入 `800`
5. 官种选择“仅官种”
6. Free 选择“仅 Free”
7. 点击“全选可见”
8. 核对已选数量
9. 点击“批量下载已选”
10. 下载完成后解压 ZIP，将其中的 `.torrent` 导入下载器

---

## 常见问题

### Q: 页面上没有出现操作面板？

1. 确认当前地址是 `https://hhanclub.net/torrents.php...`
2. 确认油猴扩展及脚本已启用
3. 刷新页面
4. 确认页面中存在种子列表和原有“下载”链接
5. 检查浏览器扩展是否禁止脚本在该站点运行

### Q: “批量下载已选”按钮无法点击？

没有选中种子时按钮处于禁用状态。请先勾选至少一个种子，或点击“全选可见”。

### Q: 所有种子都下载失败？

1. 确认 HHClub 登录状态未过期
2. 尝试点击页面原有的“下载”按钮，确认账号有下载权限
3. 刷新页面后重试
4. 检查站点是否处于维护或限流状态
5. 查看面板显示的首个错误信息
6. 打开浏览器开发者工具，在 Console 中搜索 `[HHClub 批量下载]`

脚本请求使用当前页面的同源 Cookie，不需要在脚本中填写 passkey。

### Q: 点击后只下载了一个 ZIP？

这是预期行为。选择多个种子时会打包为一个 ZIP，以避免浏览器拦截多文件自动下载。解压 ZIP 即可获得全部 `.torrent` 文件。

### Q: 选择“排除官种”或“排除 Free”后没有结果？

当前页面可能全部为官种或全部处于 Free 状态。点击“重置过滤”，再单独测试各过滤项。

### Q: 排除下载状态不准确？

脚本只读取 HHClub 页面中的 `seeding` 和 `leeching` 标记。如果本地下载器中存在任务，但站点页面没有对应状态，脚本无法识别。

### Q: 筛选会跨分页吗？

不会。脚本只筛选当前页已经加载的种子。切换分页后，新页面会重新初始化面板和选择状态。

### Q: ZIP 很大或生成较慢？

种子文件通常较小，但一次选择过多时仍需逐个请求和在内存中打包。建议按页面或过滤条件分批下载。

### Q: 如何更新脚本？

在油猴扩展中使用新版 `hhanclub-torrent-batch.user.js` 覆盖旧脚本，然后刷新 HHClub 页面。

---

## 隐私与安全

- 脚本仅在 `https://hhanclub.net/torrents.php*` 运行
- 不向第三方服务器发送种子、Cookie 或 passkey
- 种子请求直接发送到 HHClub
- ZIP 在浏览器本地内存中生成
- 下载链接可能包含个人 passkey，请勿分享链接、截图或调试日志
- 只下载和使用自己拥有授权的内容

---

## 版本信息

- **版本**：1.3.0
- **脚本文件**：`hhanclub-torrent-batch.user.js`
- **适用站点**：`https://hhanclub.net/torrents.php*`
- **批量请求间隔**：500 毫秒
- **失败重试间隔**：800 毫秒
